[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx prisma generate", "node node_modules/typescript/bin/tsc"]

[start]
cmd = "node dist/src/main.js"
